import React from 'react';
import Banner from '../Banner/Banner';

const Landing = (props) => {
	return(
		<Banner txtSide="left">
			<p className="white">Encuentra los mejores restaurantes.</p>
		</Banner>

	);
};

export default Landing;

